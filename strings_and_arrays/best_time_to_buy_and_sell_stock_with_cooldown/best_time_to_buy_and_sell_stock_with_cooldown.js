/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function (prices) {
	if(prices === null || 1 <= prices.length){
		return 0
	}
	let result = [0]
	for(let i = 0; i < prices.length; i++){
		
	}
}
